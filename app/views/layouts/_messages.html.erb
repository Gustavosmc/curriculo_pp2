
  <% flash.each do |type, message| %>
  <div class="w3-container w3-card-8 w3-large w3-margin w3-round-large <%= w3_class_for(type) %> ">
    <span onclick="this.parentElement.style.display='none'" class="w3-closebtn ">&times;</span>
    <h4><%= message %></h4>
  </div>
  <% end %>
