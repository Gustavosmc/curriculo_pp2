<%= simple_form_for(@curriculo, html: { class: 'form-horizontal' }) do |f| %>
    <%= f.error_notification %>
    <%= f.input :observacao, error: true %>
    <%= f.input :experiencia, error: true %>
    
    <%= f.button :submit %>

<% end %>
